# CHANGELOG 3.8.0

## 产品化增强

- `/sub` 新增 `format=csv` 和 `format=jsonl`，方便脚本和表格工具消费。
- 首页新增 `?plain=1` 极简模式，低端设备或脚本查看更轻。
- 首页新增“我的网络信息”卡片，展示访问国家/城市、ASN、运营商识别、推荐线路和 Cloudflare colo。
- 管理页 7 天趋势新增纯 SVG 折线图，不引入外部图表库。

## 未包含

- 未启用 IPv6 AAAA 自动同步，避免改变线上 DNS 行为。
- 未实现配置导入/导出，留待后续 admin 配置 UI 一起做。
